# DO NOT CHANGE the "init" and "install" sections below
# Download script file from GitHub
init:
  ps: |
        $ErrorActionPreference = "Stop"
        Invoke-WebRequest https://raw.githubusercontent.com/rwinlib/base/master/scripts/appveyor-tool.ps1 -OutFile "..\appveyor-tools.ps1"
        #        Invoke-WebRequest http://raw.github.com/krlmlr/r-appveyor/master/scripts/appveyor-tool.ps1 -OutFile "..\appveyor-tool.ps1"
        Import-Module '..\appveyor-tool.ps1'

install:
    #  - choco install -y miktex
    #  - choco install -y imagemagick
  - ps: |
      #      . '..\appveyor-tool.ps1'
      InstallMiktex
      pdflatex.exe --version
      Bootstrap
    #  - set "PATH=%PATH%;C:\Program Files\ImageMagick-7.0.8-Q16"
    #  - set "PATH=%PATH%;C:\Program Files\MiKTeX 2.9\bin;C:\Program Files\MiKTeX 2.9;C:\miktex\texmfs\install\miktex\bin"
    #  - initexmf --set-config-value "[MPM]AutoInstall=1"
  - ps: ls "C:\Program Files\MiKTeX 2.9"
  - ps: ls "C:\Program Files\ImageMagick-7.0.8-Q16"
  - ps: ls "C:\Program Files\MiKTeX 2.9\bin"

cache:
  - C:\RLibrary

environment:
  PKGTYPE: win.binary
  BIOC_USE_DEVEL: FALSE
  USE_RTOOLS: TRUE
  GCC_PATH: mingw_64
  R_ARCH: x64
  
  matrix:
#    - R_VERSION: devel
#      BIOC_USE_DEVEL: TRUE
#      
#    - R_VERSION: devel
#      BIOC_USE_DEVEL: TRUE
#      GCC_PATH: mingw_32
#      R_ARCH: i386    
#      
    - R_VERSION: release
    - R_VERSION: 3.5.1
    - R_VERSION: 3.4.4
    - R_VERSION: 3.4.2
    - R_VERSION: 3.4.0
      
build_script:
  - travis-tool.sh install_bioc rlang
    #  - Rscript setupEnv.R

test_script:
    - echo %PATH:;=&echo.%
    - pdflatex --version
    - convert --version
    #  - R CMD build --no-build-vignettes .
    #  - R CMD check --install-args=--build --no-vignettes --no-manual --timings *tar.gz
    #  - R -e "BiocCheck::BiocCheck(list.files(path='.', pattern='.tar.gz'))"
    #  - R -e "date(); devtools::build_vignettes(); date()"
    #  - R -e "devtools::run_examples()"

on_failure:
  - 7z a failure.zip *.Rcheck\*
  - appveyor PushArtifact failure.zip

artifacts:
  - path: '*.Rcheck\**\*.log'
    name: Logs

  - path: '*.Rcheck\**\*.out'
    name: Logs

  - path: '*.Rcheck\**\*.fail'
    name: Logs

  - path: '*.Rcheck\**\*.Rout'
    name: Logs

  - path: '\*_*.tar.gz'
    name: Bits

  - path: '\*_*.zip'
    name: Bits
